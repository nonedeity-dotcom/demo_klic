# No Burnout — архитектура

Разложено на два независимых проекта из одного демо-файла `no-burnout-app.jsx`.

```
no-burnout/
├── backend/     Node.js + TypeScript + Fastify + Prisma + PostgreSQL
└── mobile-app/  React Native + Expo + TypeScript
```

## Почему так

- Один язык (TypeScript) на фронте и бэке — общие типы (`src/types.ts` на клиенте
  соответствует моделям в `backend/prisma/schema.prisma`), меньше context-switching.
- Бэкенд нужен ради двух вещей: **аккаунт + синк между устройствами** и
  **серверная логика напоминаний** («напомни, если не отметил привычку к 21:00») —
  это невозможно сделать только локальными уведомлениями на устройстве.
- Каждый `window.storage`-ключ из демо стал таблицей в Postgres:

  | Было (localStorage/window.storage) | Стало (Prisma-модель)   |
  |---|---|
  | `habit-log-v1`           | `HabitLog`            |
  | `habits-list-v1`         | `Habit`                |
  | `triggers-v1` / `triggers-list-v1` | `TriggerLog` / `Trigger` |
  | `energy-log-v1`          | `EnergyLog`            |
  | `timer-stats-v1`         | `FocusSession`         |
  | `daily-question-v1`      | `DailyQuestion`        |
  | `celebrated-milestones-v1` | `CelebratedMilestone` |
  | — (не было) | `PushToken`, `ReminderSetting` — новое, для пушей |

## Backend: запуск

```bash
cd backend
cp .env.example .env        # укажи свою DATABASE_URL и JWT_SECRET
npm install
npx prisma migrate dev      # создаёт таблицы в Postgres
npm run dev                 # API на http://localhost:3000

# отдельным процессом — воркер, который шлёт пуш-напоминания
npm run cron:reminders
```

Нужен запущенный PostgreSQL (`docker run -p 5432:5432 -e POSTGRES_PASSWORD=pass postgres` — самый быстрый способ для локальной разработки).

## Mobile app: запуск

> **Текущий режим — только для личного использования, без бэкенда.**
> Нет ни логина, ни аккаунта: все данные (привычки, триггеры, энергия, фокус-сессии,
> вопрос дня) хранятся локально на устройстве через `AsyncStorage`, а напоминание —
> обычное ежедневное локальное уведомление (экран «Напоминание»), без серверного
> cron. `backend/` в проекте остался нетронутым на будущее — если понадобится синк
> между устройствами, экран логина и серверные push можно будет вернуть.

```bash
cd mobile-app
npm install
npx expo start
```

Дальше — отсканировать QR в приложении Expo Go (для быстрой разработки), или собрать
APK через workflow `.github/workflows/build-android.yml` (GitHub Actions → Run workflow),
готовый APK прикрепляется к GitHub Release.

## Что уже перенесено из демо

**Backend** — все эндпоинты: auth (register/login), habits + habit log (toggle,
CRUD), triggers, energy grid, focus sessions, daily question, плюс push-токены
и cron для серверных напоминаний. Сейчас не используется мобильным приложением
(см. режим выше), но готов к подключению, когда понадобится синк между устройствами.

**Mobile app** — все 6 вкладок демо — настоящие нативные экраны (`src/screens/*`),
навигация — `@react-navigation/bottom-tabs` внутри `native-stack`, данные —
`@tanstack/react-query` поверх локального `AsyncStorage`-хранилища
(`src/api/client.ts`) вместо HTTP-запросов к бэкенду.

Нижняя панель: **Отчёт · Чек-лист · Фокус · Энергия · Триггеры · Вопрос**. Отчёт
открывается первым, потому что на нём подсказка дня. Шестерёнка в правом верхнем
углу открывает **Настройки** — справочник, напоминание, лимит экранного времени и
резервные копии; восьмой вкладке внизу просто не хватило бы ширины (семь подписей
на экране 320px влезают только в 8pt, шесть — в 9pt целиком).

## Справочник и подсказки

`src/content/library.ts` — 44 подсказки в четырёх разделах (Фокус · Дофамин и
триггеры · Яма и плато · Действие вместо анализа), собранные из четырёх исходных
конспектов, на которых построено приложение. Данные зашиты в приложение: сервера
нет, а подсказку, которой нужен интернет, не прочитать на телефоне, который ты по
её же совету унёс в другую комнату.

У каждой подсказки короткая строка и абзац, который раскрывается по нажатию.
Там, где источник выдаёт мнение автора за факт (деление дофамина на
«искусственный» и «настоящий», 66 дней, исследование про IQ и доход), подсказка
несёт отдельное поле `caveat` — приложение не ручается за то, чего не может
проверить.

Поле `rotate` определяет, участвует ли подсказка в ротации: объяснение модели
(«что такое яма») читают один раз, и в качестве подсказки оно бесполезно — такие
лежат только в справочнике. Из 44 подсказок в ротации 39, и они пронумерованы
1..39.

Ротация (`src/lib/useRotatingTip.ts`) сдвигается на одну **при каждом заходе в
приложение** и после конца начинается сначала. Позиция хранится в AsyncStorage
(`KEYS.tipCursor`) и увеличивается в двух случаях: при старте JS и при возврате
из фона. Второе обязательно — Android обычно не убивает процесс, так что повторный
запуск с иконки часто не перезапускает JS, и без этого подсказка выглядела бы
застрявшей. Считается только переход `background → active`: шторка уведомлений и
системные диалоги дают `inactive → active`, и сжигать на них подсказку незачем.

Номер рядом с подсказкой — не украшение: раз выбор не случайный, а по кругу,
номер это единственный способ понять, где ты в цикле. Он же показан у подсказок в
справочнике.

Позиция в ротации намеренно **не входит** в резервную копию: это закладка одного
устройства, а не история, которую есть смысл переносить на новый телефон.

Проверено на собранном приложении: пять заходов подряд дают 1, 2, 3, 4, 5 с разными
текстами; с 39-й ротация возвращается к 1-й и к тому же тексту; чистая установка
начинается с 1, а не с 2; возврат из фона сдвигает счётчик, а событие видимости
без реального ухода в фон — нет.

## Что осталось доделать (за пределами этого скаффолда)

- Огонёк-стрик (`FlameIcon`/`flameColor`) и анимации (`TwoCurves`, конфетти при
  66 днях) — логика цвета/градиента 1:1 переносится, но SVG/анимации на
  `react-native-svg` + `react-native-reanimated` нужно дорисовать отдельно.
- Экран настройки времени напоминания (`ReminderSetting`) — эндпоинт на бэке есть
  (`PUT /push/reminder`), экран в мобильном приложении не создан.
- Обработка истёкшего JWT (refresh-токены) — сейчас токен просто живёт 30 дней.
- EAS Build конфиг для сборки под TestFlight/Play Store.
